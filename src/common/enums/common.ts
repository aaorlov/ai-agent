export enum Environment {
	DEV = "dev",
	PROD = "prod",
	TEST = "test",
}

export enum Routes {
	Root = "/",
	Health = "/health",
	Threads = "/threads",
	OpenApi = "/openapi.json",
	Docs = "/docs",
}
